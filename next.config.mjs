/** @type {import('next').NextConfig} */
const nextConfig = {
   darkMode: 'class',
  images: {
    domains: ['images.unsplash.com',"www.euttaranchal.com"],

  },
  
  
};

  
export default nextConfig;
